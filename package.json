
{
  "name": "adexclusion-enterprise",
  "version": "2.1.0",
  "description": "Edge-native Ad Exclusion Engine for High-Traffic Portals",
  "private": true,
  "scripts": {
    "build": "echo 'No build required for static assets'",
    "deploy": "wrangler pages deploy . --project-name adex-manager"
  },
  "devDependencies": {
    "wrangler": "^3.101.0",
    "@cloudflare/workers-types": "^4.20241218.0"
  }
}
