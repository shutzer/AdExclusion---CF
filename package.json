
{
  "name": "adexclusion-enterprise",
  "version": "2.1.0",
  "description": "Edge-native Ad Exclusion Engine for High-Traffic Portals",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --config wrangler.local.toml --port 8788",
    "build": "echo 'Build assets prepared'",
    "deploy": "echo 'Deployment triggered by Pages CI/CD'"
  },
  "devDependencies": {
    "wrangler": "^3.101.0",
    "@cloudflare/workers-types": "^4.20241218.0"
  }
}
